<% content_for :head, stylesheet_link_tag("grove") %>

<%= turbo_stream_from "grove" %>

<%# No local: the scene draws its own inside the cache block, so a hit never
    grows a tree to throw away. %>
<%= render "grove/scene" %>
<%= render "uplink/hud", canvas: false %>

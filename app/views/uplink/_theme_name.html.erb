<%# Its own element so the theme-set hook can replace just this. The rest of
    the HUD does not change when the desktop's theme does. %>
<span id="theme-name" class="hud__theme"><%= Omarchy.theme_name %></span>

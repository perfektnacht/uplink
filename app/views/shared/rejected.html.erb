<%= turbo_frame_tag "inspector" do %>
  <div class="form">
    <header class="form__head">
      <h3>Not saved</h3>
      <a class="form__close" href="#" data-action="inspector#close">×</a>
    </header>
    <ul class="form__errors">
      <li>The browser's session did not match. Reload the page and try again.</li>
    </ul>
  </div>
<% end %>
